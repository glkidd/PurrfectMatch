import { Api } from 'Api';


describe("getSuccessStories", () => {
    test.todo("fetches page 0 correctly");

    test.todo("fetches page 3 correctly");

    test.todo("works correctly with nonexistend page");
});

describe("submitContactInfo", () => {
    test.todo("promise resolves when connection succeeds");

    test.todo("promise rejects when connection fails");
});
